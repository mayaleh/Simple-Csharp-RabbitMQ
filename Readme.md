# C# & RabbitMQ

**RabbitMQ server with manage:**

`./docker-compose.yml`

opened port: `localhost`:

- `5672`: AMQP
- `15672`: Web manager RabbitMQ

Producer app:

`./Send`

Consumer app:

`./Receive`
